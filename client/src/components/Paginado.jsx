import React, { useState } from 'react'
import { useDispatch, useSelector } from 'react-redux'
// pagina para armar toda la logica del paginado y crear un estado global con los paises que quiero que me muestre en cada pagina. conectar con el usestate del home 
export default function Paginado() {
// const dispatch = useDispatch()
// const allrecipes = useSelector(state => state.allrecipes)

// for (let i = 0; i < 10; i++) {
//       var array = []
//       array.push(allrecipes[i])
//       console.log(array);
//       return array
// }  

    return (
        <div>
            
        </div>
    )
}
