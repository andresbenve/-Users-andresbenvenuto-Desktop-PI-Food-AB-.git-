const  axios  = require("axios")
const {Recipe, Type, recipe_type, Op} = require('../db');
const { API_KEY2, API_KEY, API_KEY3} = process.env



const getTypes = async (req, res) => {
    // Me voy a tener que traer todos los tipos de la API
}
          

module.exports = {
    getTypes
}